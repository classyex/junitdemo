server:
  port: 8080
spring:
  datasource:
    driver-class-name: org.h2.Driver
    schema: classpath:db/schema-h2.sql
    data: classpath:db/data-h2.sql
    url: jdbc:h2:mem:test
    username: root
    password: test

logging:
  level:
    com.classyex.github.junitdemo.*: DEBUG
    org.springframework.web: INFO
  file:
    name: logs/junitdemo.log

